// Alarm 0 is used to check Jon's attacking collison with the enemy

// when the count is down to zero
/*
if place_meeting(x - 1, y, obj_JonSnow)
{	
	// see if John is attacking
	if(obj_JonSnow.sprite_index == spr_attack_normal_right)
	{
		if(obj_JonSnow.image_index = 8)
			healthValue -= obj_controller.sword_damage_current;
		alarm_set(0,17);
	}
}

else
	alarm_set(0,17);
*/